version: 2

models:
  - name: dim_clients
    description: Clients dimension table.
    columns: 
      - name: customer_id
        description: primary key for dim_clients.
        tests:
          - unique
          - not_null
      - name: person_id
        description: ID for each credit card.
      - name: creditcard_id
        description: Type of each credit card.
      - name: territory_id
        description: Name of each client.
      - name: full_name
      - name: cardtype